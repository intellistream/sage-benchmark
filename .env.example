# sage-benchmark 环境变量模板
# 复制此文件为 .env，填入真实值后使用：
#   cp .env.example .env
#
# ⚠️ 切勿将 .env 提交到 git（已加入 .gitignore）

# =============================================================================
# Hugging Face 配置
# =============================================================================

# HF 写权限 token（上传结果到 intellistream/sage-benchmark-results 需要）
# 获取方式：https://huggingface.co/settings/tokens
# 用途：手动上传时使用（标准 git push 流程由 GitHub Secrets 中的 token 负责，无需此项）
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# HF endpoint（中国用户推荐设置为 mirror 加速）
# 默认：https://hf-mirror.com
# 可选：https://huggingface.co
HF_ENDPOINT=https://hf-mirror.com
